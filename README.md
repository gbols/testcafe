to run this project you can either choose to install testcafe globally or locally.
to install globally
`npm install -g testcafe`

or locally
`run npm init`
`npm install testcafe`

to run the script 
`testcafe chrome test.js`